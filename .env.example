# Copia este archivo a .env y rellena los valores.
# No subas .env al repositorio (está en .gitignore).

# URL pública del Google Sheet en formato CSV (Publicar en la web → publicar como CSV y pegar aquí).
# Ejemplo:
# VITE_SHEET_URL=https://docs.google.com/spreadsheets/d/e/TU_ID/pub?output=csv
# Sin espacios ni comillas. Ejemplo:
# VITE_SHEET_URL=https://docs.google.com/spreadsheets/d/e/XXX/pub?output=csv
VITE_SHEET_URL=
